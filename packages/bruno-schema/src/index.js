const {
  collectionSchema,
  itemSchema,
  environmentSchema,
  environmentsSchema,
  authModeSchema,
  authModesSchema
} = require('./collections');

module.exports = {
  itemSchema,
  environmentSchema,
  environmentsSchema,
  authModeSchema,
  authModesSchema,
  collectionSchema
};
