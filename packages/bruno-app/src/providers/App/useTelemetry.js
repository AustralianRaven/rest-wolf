/**
 * Telemetry has been completely disabled for privacy and security compliance.
 */

// No-op telemetry hook - all tracking has been removed
const useTelemetry = ({ version }) => {
  // Telemetry disabled - no tracking occurs
};

export default useTelemetry;
